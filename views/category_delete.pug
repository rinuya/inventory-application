extends layout

block content
  h1 #{title}: #{category.name}

  if items.length

    p #[strong Delete the following items before attempting to delete this book.]

    div(style='margin-left:20px;margin-top:20px')

      h4 Items

      dl
      each item in items
        dt
          a(href=item.url) #{item.name}
        dd #[strong Number in Stock:] #{item.numberinstock}
        dd #[strong Price:] #{item.price}

  else
    p Do you really want to delete this Category?

    form(method='POST' action='')
      div.form-group
        input#categoryid.form-control(type='hidden',name='categoryid', required='true', value=category._id )

      button.btn.btn-primary(type='submit') Delete
